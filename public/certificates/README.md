# Certificates

Optional: place certificate PDFs/images here (e.g. `aws-saa.pdf`) and set the
matching `url` in `src/data/certifications.ts` to `/certificates/<file>` to
activate each "View Certificate" button. No certificate URLs are invented — the
buttons stay disabled until you provide a real link.
