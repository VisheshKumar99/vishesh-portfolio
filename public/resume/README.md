# Resume

Place the resume PDF here as:

    vishesh_go.pdf

The "Download Resume" buttons across the site link to `/resume/vishesh_go.pdf`.
Until the real file is added, the download link will 404 — drop the PDF in this
folder to activate it. The filename is referenced in `src/data/profile.ts`
(`profile.resumeUrl`).
